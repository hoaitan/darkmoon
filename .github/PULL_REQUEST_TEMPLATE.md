## Summary

## Screenshots — required for any UI/UX change

Run `yarn capture` and paste the relevant before/after pairs from `playwright/screenshots/` below
(popup, options page, on-page notification, filter appearance, icon, ...). Delete this section
entirely if this PR has no visible UI/UX change.

| Before | After |
| ------ | ----- |
|        |       |

## Checklist

- [ ] `yarn verify` passes locally (typecheck, lint, test, build)
- [ ] Screenshots attached above, if this PR changes anything user-visible
- [ ] Loaded the unpacked `dist/` build in `chrome://extensions` and manually sanity-checked the change
